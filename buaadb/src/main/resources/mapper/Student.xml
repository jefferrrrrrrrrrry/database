<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.buaadb.mapper.StudentMapper">
    <select id="findAll" resultType="com.example.buaadb.entity.Course">
        SELECT * FROM Course
    </select>

    <select id="login" resultType="com.example.buaadb.entity.Student">
        SELECT * FROM Student WHERE Sno = #{username} and Spassword = #{password}
    </select>

    <select id="findStudent" resultType="com.example.buaadb.entity.Student">
        SELECT * FROM STUDENT WHERE sname like concat('%', #{sname}, '%') and sno like concat('%', #{sno}, '%')
    </select>

    <select id="findCourse" resultType="com.example.buaadb.entity.Course">
        SELECT Course.Cno, Cname, Cpos, Tno, Tname, Ctype, Ccredit, Ccapacity, Cremain
        FROM (Course JOIN Sel ON (Course.Cno = Sel.Cno AND Sel.Sno = #{Sno}))
    </select>
</mapper>

